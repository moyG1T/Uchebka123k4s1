<UserControl x:Class="Uchebka123k4s1.Views.FailureView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Uchebka123k4s1.Views" xmlns:viewmodels="clr-namespace:Uchebka123k4s1.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:FailureViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <Grid>
            <Border Background="{StaticResource Blue}"/>
            <TextBlock Text="{Binding InteractionName}" Foreground="White" HorizontalAlignment="Center"/>
            <Button Command="{Binding GoBackCommand}" Content="Назад" HorizontalAlignment="Left" Style="{StaticResource TextButton}"/>
            <Button Command="{Binding LogoutCommand}" Content="Выйти" HorizontalAlignment="Right" Style="{StaticResource TextButton}"/>
        </Grid>

        <ScrollViewer Grid.Row="1" Grid.RowSpan="2">
            <StackPanel Orientation="Vertical" Margin="0 10" HorizontalAlignment="Center" VerticalAlignment="Top">
                <StackPanel Margin="0 0 0 10" Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="Название"/>
                    <TextBox Text="{Binding Failure.Title}"/>
                </StackPanel>
                <StackPanel Margin="0 0 0 10" Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="Описание"/>
                    <TextBox Text="{Binding Failure.Subtitle}"/>
                </StackPanel>

                <StackPanel Margin="0 0 0 10" Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="Оборудование"/>
                    <ComboBox SelectedItem="{Binding SelectedHardware}"
                              ItemsSource="{Binding Hardwares}"
                              IsEditable="True"
                              DisplayMemberPath="Title"/>
                </StackPanel>
                
                <StackPanel Margin="0 0 0 10" Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="Время начала сбоя"/>
                    <TextBox Text="{Binding Failure.Timestamp, ConverterCulture=ru}"/>
                </StackPanel>
                <StackPanel Margin="0 0 0 10" Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="Время починки сбоя"/>
                    <TextBox Text="{Binding Failure.FixTimestamp, ConverterCulture=ru}"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <Button Grid.Row="2" Content="Сохранить"
                Command="{Binding ApplyChangesCommand}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Style="{StaticResource ElevatedButton}"/>
    </Grid>
</UserControl>
